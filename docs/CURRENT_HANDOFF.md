# 南枫批量改名当前交接

## 2026-09-15 Windows v1.1.6

- 本次交付：新增“关于软件”入口与关于页，展示产品说明、版本 `1.1.6`、开发时间、开发者信息和 GitHub 更新地址。
- 验证：30 项自动测试、compileall、凭据模式扫描、发布合同 `14/14`、PyInstaller 单文件与 Inno Setup 构建通过；最终 EXE 已正常创建“南枫批量改名”窗口且进程响应正常。
- GitHub Release `v1.1.6` 已发布，安装包与 SHA-256 均已上传；远端安装包下载回验 SHA-256 一致。未覆盖本机已安装版本，未操作用户文件。

## 2026-09-15 关于软件信息

- 侧栏“快捷入口”新增“关于软件”，打开独立模态窗口；不修改文件列表、规则、预览或日志。
- 关于页参照用户确认图片：居中“关于”标题、纯白圆角信息卡、产品/版本/开发者三段。实际信息为 `Desktop 版 1.1.5`、开发时间 `2026-09-15 22:59` 与 GitHub `nanzhufeng/NanfengBatchRenamer-Windows`。
- 定向 UI 合同涵盖入口、当前版本、构建时间、仓库文本和卡片边界；本轮未打包、安装或发布。

## 2026-09-10 Windows v1.1.5

- 本次交付：包含扩展名全局范围、ts/flv/wmv/m4v 筛选、单行筛选自动缩小字号与间距。
- 29 项测试、compileall、图标合同、PyInstaller 与 Inno Setup 构建通过；脱敏 README 预览已刷新。
- 构建脚本隔离 PATH，避免混入外部工具 DLL；最终 EXE 已验证正常创建窗口并响应，随后关闭验证窗口。
- 未覆盖本机已安装版本，未操作用户文件；安装包供用户升级。

## 2026-09-09 扩展名改名范围

- 已在规则链标题旁增加“包含扩展名”，默认关闭，勾选后所有文本规则统一处理完整文件名。
- 示例：替换 mkv1 为 mkv；或删除结尾 1 个字符同时修正 .mkv1 与 .mp41。
- 验证：28 项 unittest 全部通过，compileall 通过；包含 UI 实时切换、默认兼容、所有文本规则、冲突、临时文件执行与撤销。
- 本轮仅修改源码和文档，未重新打包、安装、发布，也未操作用户文件。已安装版仍为此前发布版本。

## 历史发布任务

- 目标：修复 Windows 桌面与第三方桌面整理器复用旧方形图标缓存，并发布 Windows `v1.1.4`。
- 发布基准：GitHub Release `v1.1.4`，主资产为 Inno Setup 7 x64 安装程序。
- 已修改范围：图标透明圆角与校验资产，以及 `v1.1.0` 已有的事务执行、性能和分辨率能力。
- 未修改且受保护：用户文件、历史 Git 提交、历史版本标签和无关项目；旧 `v1.1.3` Release 已由 `v1.1.4` 替换。

## 验证等级

- 已实现：事务执行/撤销、大小写中转、失败整批恢复、恢复清单、等比缩放、性能与分辨率资产。
- 定向契约：`python -m unittest discover -s tests -v` 共 24 项通过。
- 图标合同：仓库保留用户确认原图；仅低饱和暖白区域转为中性灰，其他 RGB 不变；Alpha 圆角半径固定约 19%；ICO 包含 10 档尺寸。
- 性能：50,000 条无内存追踪预览约 1.44 秒；正式 `tracemalloc` 报告为 6.83 秒，见 `docs/performance-baseline.json`。
- 分辨率：`1280×720`、`1366×768`、`1600×900`、`1920×1080`、`2200×1152` 离屏截图与文字边界检查 5/5 通过；Windows 原生 Qt 的 `1280×720` 目标窗口为 `1280×670`，中文标签单行且边界检查通过。
- 构建：PyInstaller 6.21.0 与 Inno Setup 7.0.2 x64 通过，主资产 `NanfengBatchRenamer-Windows-v1.1.4-Setup.exe`。
- 快捷方式：安装目录使用 `app_icon-v1.1.4.ico`，桌面与开始菜单快捷方式显式引用该版本化路径，避免复用旧缓存键。
- 图标：窗口、EXE 和 Inno Setup 安装器统一使用明显透明大圆角图标，文件卡片由暖白调整为中性灰白。
- 安装：当前用户正式安装已升级到 `1.1.4`；版本化 ICO 与构建源摘要一致，窗口标题正常，测试进程已关闭。
- 发布合同：Inno Setup 安装包命名与仓库平台合同 11/11 通过。
- 真实 Windows：安装版窗口标题为“南枫批量改名”；实际桌面 48px 图标四角已显示桌面背景，圆角轮廓通过。小智桌面 `XZDesktop64.exe` 首次仍绘制旧方形缓存，重启其主进程后重新取用版本化 ICO；未删除其缓存或布局数据。
- GitHub 预览：`docs/images/app-preview.png` 由当前 `MainWindow`、固定脱敏影视演示数据和真实预览规则生成，尺寸 `1920×1005`；README 首屏与 `v1.1.4` Release 说明直接展示。
- 真实文件：仅在测试创建的 Windows 临时目录执行普通、大小写、互换、失败回滚与撤销；未操作用户目录。
- 待验证：真实双显示器/不同 DPI 跨屏拖动、屏幕阅读器、高对比模式、网络共享文件系统、断电恢复、50,000 行真实表格交互。

## 继续入口

后续任务先读：

1. `AGENTS.md`
2. `docs/context.md`
3. `docs/domain-rules.md`
4. `docs/experience-audit.md`
5. `docs/superpowers/specs/2026-07-19-reliability-responsive-design.md`
6. 本文件

没有自动任务。下一轮应先报告当前状态；只有用户明确要求时才提交、推送、重新打包或创建新 Release。
