# 南枫批量改名当前交接

## 当前任务

- 目标：在用户确认图标四周增加轻微透明圆角，并发布 Windows `v1.1.2`。
- 发布基准：GitHub Release `v1.1.2`，主资产为 Inno Setup 7 x64 安装程序。
- 已修改范围：图标透明圆角与校验资产，以及 `v1.1.0` 已有的事务执行、性能和分辨率能力。
- 未修改且受保护：用户文件、历史 Git 提交、历史版本标签和无关项目；旧 `v1.1.1` Release 已由 `v1.1.2` 替换。

## 验证等级

- 已实现：事务执行/撤销、大小写中转、失败整批恢复、恢复清单、等比缩放、性能与分辨率资产。
- 定向契约：`python -m unittest discover -s tests -v` 共 23 项通过。
- 图标合同：仓库保留用户确认原图；仅低饱和暖白区域转为中性灰，其他 RGB 不变；Alpha 形成轻微圆角；ICO 包含 10 档尺寸。
- 性能：50,000 条无内存追踪预览约 1.44 秒；正式 `tracemalloc` 报告为 6.83 秒，见 `docs/performance-baseline.json`。
- 分辨率：`1280×720`、`1366×768`、`1600×900`、`1920×1080`、`2200×1152` 离屏截图与文字边界检查 5/5 通过；Windows 原生 Qt 的 `1280×720` 目标窗口为 `1280×670`，中文标签单行且边界检查通过。
- 构建：PyInstaller 6.21.0 与 Inno Setup 7.0.2 x64 通过，主资产 `NanfengBatchRenamer-Windows-v1.1.2-Setup.exe`。
- 图标：窗口、EXE 和 Inno Setup 安装器统一使用轻微透明圆角图标，文件卡片由暖白调整为中性灰白。
- 安装：当前用户隔离目录安装成功，安装后 EXE 摘要一致，窗口标题与响应正常，卸载后目录和注册表项均清除。
- 发布合同：Inno Setup 安装包命名与仓库平台合同 11/11 通过。
- 真实 Windows：BAT 启动窗口标题为“南枫批量改名”，进程响应正常，错误日志 0 字节新增；测试进程已关闭。
- 真实文件：仅在测试创建的 Windows 临时目录执行普通、大小写、互换、失败回滚与撤销；未操作用户目录。
- 待验证：真实双显示器/不同 DPI 跨屏拖动、屏幕阅读器、高对比模式、网络共享文件系统、断电恢复、50,000 行真实表格交互。

## 继续入口

后续任务先读：

1. `AGENTS.md`
2. `docs/context.md`
3. `docs/domain-rules.md`
4. `docs/experience-audit.md`
5. `docs/superpowers/specs/2026-07-19-reliability-responsive-design.md`
6. 本文件

没有自动任务。下一轮应先报告当前状态；只有用户明确要求时才提交、推送、重新打包或创建新 Release。
